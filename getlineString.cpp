#include <iostream>
using namespace std;
main(){
    string x;
    cout<<"enter full name : ";
    getline(cin,x);     //getline(cin,var_name) is used to take input of more than one words string.
    cout<<x;
}